procedure main {
    x = 10;
    y = 20;
    z = 50;
    i = 100;
    j = 50;
    x = x + y + z;
    y = z * 2;
    while(i > 0) {
        y = x + i * 2
    }
    print x;
    read y;
    while(j >
 0) {
       x = x + 1;
       while(i > 0) {
          y = z * 5;
          read z;
       }
       j--;
    }

   if(x > y) {
       if(x > 0) {
         y = y - 2;
       }else{
         y = y + 2;
       }
   } else {
      print y;
      z = y * 2;
   }
}
